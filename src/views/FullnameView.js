import React from "react";
import { StyleSheet, Text, View } from "react-native";

const FullnameView = () => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default FullnameView;

const styles = StyleSheet.create({});
